 تكلفة التوصيل حسب المسافة
3. تتبع حالة الطلب (قيد التحضير، في الطريق، تم التسليم)
4. إشعارات SMS للعميل
5. تقييم خدمة التوصيل

### الأمان:
- التحقق من صحة رقم الموبايل
- CSRF Token في جميع الطلبات
- Validation على جانب السيرفر

## الدعم الفني

للمشاكل أو الاستفسارات:
- تحقق من ملف `storage/logs/laravel.log`
- تأكد من وجود جدول `acc_head`
- تأكد من صحة route `pos.search-customer`
حث تلقائياً
- يظهر "جاري البحث..." أثناء البحث

### 4. ملء البيانات
- إذا كان العميل موجود: البيانات تظهر تلقائياً
- إذا كان جديد: أدخل الاسم والعنوان

### 5. تأكيد الطلب
```
[✓ تأكيد الطلب]
```

### 6. إكمال الطلب
- أضف الأصناف
- اضغط "دفع وحفظ"

## المزايا

✅ بحث سريع وتلقائي
✅ تجربة مستخدم سلسة
✅ لا حاجة لإدخال البيانات مرة أخرى
✅ إنشاء عملاء جدد تلقائياً
✅ حفظ بيانات الديليفري مع الطلب
✅ إمكانية تعديل بيانات العملاء الموجودين

## ملاحظات تطويرية

### للتحسين المستقبلي:
1. إضافة خريطة لتحديد الموقع
2. حسابsearchCustomerByPhone()`
- تعديل method: `saveOrder()` لمعالجة بيانات الديليفري

### 3. Modules/POS/resources/views/pos/barcode.blade.php
- تحديث Modal الديليفري
- إضافة JavaScript للبحث التلقائي
- إضافة دالة `confirmDelivery()`
- إضافة دالة `openDeliveryModal()`

## الاستخدام

### 1. اختيار نوع الطلب
```
[تيك أواي] [طاولة] [🏍️ دليفري]
                      ↑ اضغط هنا
```

### 2. إدخال رقم الموبايل
```
رقم العميل: [010___________]
            ↑ ابدأ الكتابة
```

### 3. انتظر نتيجة البحث
- بعد 3 أرقام يبدأ الب
{
  "phone": "01000036523"
}
```

**Response (موجود):**
```json
{
  "success": true,
  "found": true,
  "customer": {
    "id": 123,
    "name": "نور",
    "phone": "01000036523",
    "address": "سملوط"
  }
}
```

**Response (غير موجود):**
```json
{
  "success": true,
  "found": false,
  "message": "عميل جديد - يرجى إدخال بياناته"
}
```

## الملفات المعدلة

### 1. Modules/POS/routes/web.php
- إضافة route: `POST /pos/search-customer`

### 2. Modules/POS/app/Http/Controllers/POSController.php
- إضافة method: `d: معرف العميل
- aname: اسم العميل
- code: كود العميل (122xxxxx)
- mobile: رقم الموبايل
- phone: رقم الهاتف
- address: العنوان
- parent_id: 122 (للعملاء)
- is_basic: 0
- isdeleted: 0
```

### جدول ot_head (الطلبات)
```sql
- age: نوع الطلب (3 = ديليفري)
- acc1: معرف العميل
- info: معلومات إضافية (تحتوي على بيانات الديليفري)
```

### صيغة حقل info للديليفري
```
"ديليفري - نور | موبايل: 01000036523 | عنوان: سملوط"
```

## المسارات (Routes)

### POST /pos/search-customer
البحث عن عميل برقم الموبايل

**Request:**
```json─────────┴──────────────┘
```

### 3. حفظ الطلب
```javascript
confirmDelivery()
  ↓
التحقق من البيانات
  ↓
إنشاء JSON object
  ↓
حفظ في hidden input
  ↓
إرسال مع النموذج
  ↓
POSController::saveOrder()
  ↓
┌──────────────┬─────────────┐
│ عميل جديد    │ عميل موجود  │
├──────────────┼─────────────┤
│ إنشاء سجل    │ استخدام ID  │
│ في acc_head  │ الموجود     │
└──────────────┴─────────────┘
  ↓
حفظ الطلب في ot_head
  ↓
إضافة معلومات الديليفري في حقل info
```

## قاعدة البيانات

### جدول acc_head (العملاء)
```sql
- iيل] [✓ تأكيد]   │
└─────────────────────────────────────┘
```

## التدفق البرمجي

### 1. فتح Modal
```javascript
openDeliveryModal()
  ↓
- مسح البيانات السابقة
- إخفاء زر "حفظ التعديل"
- عرض Modal
```

### 2. البحث عن العميل
```javascript
كتابة رقم الموبايل
  ↓
انتظار 500ms (debounce)
  ↓
AJAX Request → /pos/search-customer
  ↓
┌─────────────┬──────────────┐
│ موجود       │ غير موجود    │
├─────────────┼──────────────┤
│ ملء البيانات│ حقول فارغة   │
│ تنبيه أخضر  │ تنبيه أزرق   │
│ زر التعديل  │ إدخال يدوي   │
└────            │
│  [✓ تم العثور على العميل]          │
│                                     │
│  رقم الموبايل                       │
│  [01000036523] (readonly)           │
│                                     │
│  اسم العميل                         │
│  [نور]                              │
│                                     │
│  العنوان                            │
│  [سملوط]                            │
│                                     │
├─────────────────────────────────────┤
│  [إلغاء] [حفظ التعدعثور على العميل:
- يظهر تنبيه أزرق: "➕ عميل جديد - يرجى إدخال بياناته"
- يتم ملء رقم الموبايل تلقائياً
- يطلب من المستخدم إدخال:
  - اسم العميل
  - العنوان
- عند الحفظ، يتم إنشاء عميل جديد في قاعدة البيانات

## واجهة المستخدم

### Modal الديليفري
```
┌─────────────────────────────────────┐
│  🏍️ بيانات العميل - دليفري         │
├─────────────────────────────────────┤
│  رقم العميل                         │
│  [___________] (بحث تلقائي)         │
│  سيتم البحث تلقائياً بعد 3 أرقام    │
│                          التلقائي عن العملاء وإضافة عملاء جدد.

## الميزات الرئيسية

### 1. البحث التلقائي عن العملاء
- البحث يبدأ تلقائياً بعد كتابة 3 أرقام
- البحث في جدول `acc_head` (العملاء الذين code يبدأ بـ 122)
- البحث في حقول: `mobile` و `phone`

### 2. حالتان للعميل

#### أ. عميل موجود
عند العثور على العميل:
- يظهر تنبيه أخضر: "✓ تم العثور على العميل"
- يتم ملء البيانات تلقائياً:
  - رقم الموبايل
  - اسم العميل
  - العنوان
- يظهر زر "حفظ التعديل" للسماح بتحديث البيانات
- يمكن تعديل الاسم والعنوان

#### ب. عميل جديد
عند عدم ال# ميزة الديليفري في نظام POS

## نظرة عامة
تم تطوير نظام ديليفري متكامل يتضمن البحث