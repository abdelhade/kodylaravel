# 📋 دليل إعدادات نقطة البيع (POS Config)

## 📁 الملف: `pos_config.json`

هذا الملف يحتوي على جميع إعدادات نظام نقطة البيع ويمكن تعديله بسهولة دون الحاجة لتعديل الكود.

---

## ⚙️ الإعدادات المتاحة

### 1. إعدادات باركود الميزان (`scale_barcode`)

```json
{
  "scale_barcode": {
    "enabled": true,
    "prefix": "200",
    "barcode_length": 13,
    "item_code_start": 3,
    "item_code_length": 4,
    "weight_start": 7,
    "weight_length": 5,
    "weight_divisor": 1000,
    "description": "إعدادات باركود الميزان - الباركود: 200XXYYYYY#"
  }
}
```

#### شرح المتغيرات:

| المتغير | الوصف | مثال |
|---------|-------|------|
| `enabled` | تفعيل/تعطيل باركود الميزان | `true` أو `false` |
| `prefix` | الأرقام الثابتة في بداية الباركود | `"200"` |
| `barcode_length` | إجمالي طول الباركود | `13` |
| `item_code_start` | موضع بداية كود الصنف | `3` (بعد الـ prefix) |
| `item_code_length` | عدد أرقام كود الصنف | `4` |
| `weight_start` | موضع بداية الوزن | `7` |
| `weight_length` | عدد أرقام الوزن | `5` |
| `weight_divisor` | المقسوم عليه لتحويل الوزن | `1000` (لتحويل الجرام لكيلو) |

#### 📌 مثال باركود الميزان:

```
2001001001251
│││││││││││││└─ رقم التأكيد (1)
││││││└┴┴┴┴─── الوزن: 00125 → 2.125 كجم
│││└┴┴┴────── كود الصنف: 1001 → 1
└┴┴───────── البادئة الثابتة: 200
```

---

### 2. إعدادات العرض (`display`)

```json
{
  "display": {
    "items_per_row": 4,
    "show_item_images": true,
    "default_icon": "fa-utensils"
  }
}
```

| المتغير | الوصف | القيم الممكنة |
|---------|-------|---------------|
| `items_per_row` | عدد الأصناف في الصف الواحد | `3`, `4`, `6` |
| `show_item_images` | إظهار صور الأصناف | `true` أو `false` |
| `default_icon` | الأيقونة الافتراضية عند عدم وجود صورة | `"fa-utensils"`, `"fa-box"`, إلخ |

---

### 3. إعدادات الألوان (`colors`)

```json
{
  "colors": {
    "primary": "#0a7ea4",
    "primary_rgb": "10, 126, 164"
  }
}
```

| المتغير | الوصف | مثال |
|---------|-------|------|
| `primary` | اللون الأساسي بصيغة HEX | `"#0a7ea4"` (أزرق غامق) |
| `primary_rgb` | اللون الأساسي بصيغة RGB | `"10, 126, 164"` |

#### 🎨 ألوان مقترحة:

| اللون | HEX | RGB |
|-------|-----|-----|
| أزرق غامق | `#0a7ea4` | `10, 126, 164` |
| أحمر | `#dc3545` | `220, 53, 69` |
| أخضر | `#28a745` | `40, 167, 69` |
| برتقالي | `#fd7e14` | `253, 126, 20` |

---

### 4. إعدادات السلوك (`behavior`)

```json
{
  "behavior": {
    "auto_focus_barcode": true,
    "clear_barcode_after_scan": true,
    "increment_existing_items": true
  }
}
```

| المتغير | الوصف | القيم الممكنة |
|---------|-------|---------------|
| `auto_focus_barcode` | التركيز التلقائي على حقل الباركود | `true` أو `false` |
| `clear_barcode_after_scan` | مسح حقل الباركود بعد المسح | `true` أو `false` |
| `increment_existing_items` | زيادة الكمية للأصناف الموجودة | `true` أو `false` |

---

## 🔧 كيفية التعديل

### مثال 1: تغيير بادئة باركود الميزان من `200` إلى `250`

```json
{
  "scale_barcode": {
    "prefix": "250"
  }
}
```

### مثال 2: تغيير اللون الأساسي إلى أحمر

```json
{
  "colors": {
    "primary": "#dc3545",
    "primary_rgb": "220, 53, 69"
  }
}
```

### مثال 3: تعطيل باركود الميزان

```json
{
  "scale_barcode": {
    "enabled": false
  }
}
```

---

## ⚠️ ملاحظات هامة

1. **بعد أي تعديل:** قم بعمل Refresh للصفحة (Ctrl + F5) لتحميل الإعدادات الجديدة
2. **التأكد من صحة JSON:** استخدم أداة مثل [jsonlint.com](https://jsonlint.com/) للتحقق من صحة التنسيق
3. **النسخ الاحتياطي:** احتفظ بنسخة احتياطية من الملف قبل التعديل
4. **الإعدادات الافتراضية:** في حالة فشل تحميل الملف، سيتم استخدام الإعدادات الافتراضية المضمنة في الكود

---

## 🐛 استكشاف الأخطاء

### المشكلة: التعديلات لا تظهر

**الحل:**
1. افتح Developer Console (F12)
2. تأكد من رؤية: `✅ POS Config loaded:`
3. قم بعمل Hard Refresh (Ctrl + Shift + R)

### المشكلة: خطأ في تحميل الإعدادات

**الحل:**
1. تحقق من صحة تنسيق JSON
2. تأكد من وجود الملف `pos_config.json` في نفس مجلد `pos_barcode.php`
3. تحقق من الـ Console لرؤية رسالة الخطأ

---

## 📞 الدعم الفني

في حالة وجود أي مشاكل أو استفسارات، يمكنك:
1. فحص Console في المتصفح (F12)
2. البحث عن رسالة `🔢 Scale Barcode Detected:` للتأكد من قراءة باركود الميزان بشكل صحيح

---

**آخر تحديث:** <?php echo date('Y-m-d'); ?>

