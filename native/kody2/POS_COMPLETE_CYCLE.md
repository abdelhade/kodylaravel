# 🔄 السايكل الكاملة لنظام POS مع الطاولات

## 📊 نظرة عامة على النظام

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   tables    │ ←──→ │ pos_barcode │ ←──→ │ pos_tables  │
│   .php      │      │    .php     │      │    .php     │
└─────────────┘      └─────────────┘      └─────────────┘
      ↓                     ↓                     ↓
┌─────────────────────────────────────────────────────────┐
│              قاعدة البيانات (MySQL)                     │
│  tables | ot_head | fat_details | myitems | acc_head   │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 السيناريو الأول: طلب جديد على طاولة فارغة

### **الخطوة 1: عرض الطاولات** 📋
```
المستخدم → يفتح tables.php
         ↓
عرض جميع الطاولات من قاعدة البيانات:
- 🟢 أخضر = فارغة (table_case = 0)
- 🔴 أحمر = مشغولة (table_case = 1)
```

**الكود:**
```php
// في tables.php - جلب الطاولات
$tables_query = "SELECT * FROM tables WHERE isdeleted = 0 ORDER BY id ASC";
$tables_result = $conn->query($tables_query);

while ($table = $tables_result->fetch_assoc()) {
    $bg_color = ($table['table_case'] == 0) ? 'bg-sky-400' : 'bg-red-300';
    // عرض الطاولة
}
```

---

### **الخطوة 2: اختيار الطاولة** 👆
```
المستخدم → ينقر على طاولة 5 (فارغة)
         ↓
يظهر معلومات الطاولة:
- اسم الطاولة: "طاولة 5"
- الحالة: فارغة
- الإجماليات: 0.00
```

**الكود:**
```javascript
// عند النقر على طاولة
function selectTable(tableId, tableName) {
    $('#selected_table_id').val(tableId);
    $('#table_name').val(tableName);
    
    // جلب طلب الطاولة (إن وجد)
    loadTableOrder(tableId, tableName);
}
```

---

### **الخطوة 3: فتح POS** 🖥️
```
المستخدم → ينقر "طلب جديد"
         ↓
ينتقل إلى: pos_barcode.php?table=طاولة%205
         ↓
يتم تحديد:
- نوع الطلب = "طاولة" ✓ (تلقائياً)
- اسم الطاولة = "طاولة 5" (تلقائياً)
- الطلب جديد (فارغ)
```

**الكود:**
```javascript
// في pos.js
const urlParams = new URLSearchParams(window.location.search);
const tableName = urlParams.get('table');
if (tableName) {
    $('#age2').prop('checked', true);  // اختيار "طاولة"
    $('#table_name').val(decodeURIComponent(tableName)).show();
}
```

---

### **الخطوة 4: إضافة الأصناف** 🍽️

#### **طريقة 1: النقر على الأصناف**
```
المستخدم → ينقر "شاي" (15.00 ج)
         ↓
AJAX → جلب بيانات الصنف من قاعدة البيانات
         ↓
إضافة الصنف للجدول:
┌───┬─────────┬──────┬───────┬────────┬───┐
│ م │ الاسم   │ كمية │ سعر   │ القيمة │ X │
├───┼─────────┼──────┼───────┼────────┼───┤
│ 1 │ شاي     │  1   │ 15.00 │ 15.00  │ X │
└───┴─────────┴──────┴───────┴────────┴───┘
```

**الكود:**
```javascript
// في pos.js
$('.itemButton').on('click', function() {
    const barcode = $(this).attr('itemid');
    fetchData(barcode);  // جلب بيانات الصنف عبر AJAX
});

function addNewRow(itemData, price) {
    const newRow = `
        <tr data-itemid="${itemData.barcode}">
            <td>${rownum}</td>
            <td>${itemData.iname}</td>
            <td><input type="number" value="1" name="itmqty[]"></td>
            <td><input type="number" value="${price}" name="itmprice[]"></td>
            <td><input readonly value="${price}" name="itmval[]"></td>
            <td><button class="btn btn-danger">X</button></td>
        </tr>
    `;
    $('#itemData').append(newRow);
    updateTotal();  // حساب الإجمالي
}
```

#### **طريقة 2: الباركود**
```
المستخدم → يمسح باركود "123456"
         ↓
AJAX → البحث في قاعدة البيانات
         ↓
إضافة الصنف للطلب تلقائياً
```

**الكود:**
```javascript
$('#barcodeInput').on('keypress', function(e) {
    if (e.which === 13) {
        const barcode = $(this).val();
        fetchData(barcode);
        $(this).val('');
    }
});
```

---

### **الخطوة 5: الحسابات التلقائية** 🧮
```
عند إضافة أصناف:
┌──────────────┬──────────┐
│ الإجمالي     │ 50.00 ج  │
│ خصم (10%)    │  5.00 ج  │
│ الصافي       │ 45.00 ج  │
└──────────────┴──────────┘

الحسابات تتم تلقائياً عند:
- إضافة صنف
- تعديل الكمية
- تعديل السعر
- إضافة خصم
```

**الكود:**
```javascript
const updateTotal = () => {
    let total = 0;
    $('.subtotal').each(function() {
        total += parseFloat($(this).val());
    });
    $('#total').val(total.toFixed(2));
    updateNetValue();
};

const updateNetValue = () => {
    const total = parseFloat($('#total').val());
    const discount = parseFloat($('#discount').val());
    const netValue = total - discount;
    $('#net_val').val(netValue.toFixed(2));
};
```

---

### **الخطوة 6: حفظ الطلب** 💾
```
المستخدم → ينقر "حفظ"
         ↓
Form → do/doadd_invoice.php
         ↓
حفظ في قاعدة البيانات:

1. جدول ot_head (رأس الطلب):
   - pro_tybe = 9 (POS)
   - info = "طاولة 5"
   - fat_total = 50.00
   - fat_disc = 5.00
   - pro_value = 45.00
   
2. جدول fat_details (تفاصيل الأصناف):
   - pro_id = 123 (معرف الطلب)
   - item_id = 45 (الشاي)
   - qty = 2
   - price = 15.00

3. تحديث جدول tables:
   - table_case = 1 (مشغولة)
   - current_order_id = 123
```

**الكود المطلوب في do/doadd_invoice.php:**
```php
// حفظ رأس الطلب
$table_name = $_POST['table'] ?? null;
$table_id = $_POST['table_id'] ?? null;

$stmt = $conn->prepare("INSERT INTO ot_head 
    (pro_tybe, info, fat_total, fat_disc, pro_value, pro_date, store_id, emp_id) 
    VALUES (9, ?, ?, ?, ?, ?, ?, ?)");
$stmt->execute([$table_name, $total, $discount, $net, $date, $store_id, $emp_id]);

$order_id = $conn->insert_id;

// حفظ الأصناف
foreach ($items as $item) {
    $stmt = $conn->prepare("INSERT INTO fat_details 
        (pro_id, item_id, qty, price) VALUES (?, ?, ?, ?)");
    $stmt->execute([$order_id, $item['id'], $item['qty'], $item['price']]);
}

// تحديث حالة الطاولة
if ($table_id) {
    $stmt = $conn->prepare("UPDATE tables 
        SET table_case = 1, current_order_id = ? WHERE id = ?");
    $stmt->execute([$order_id, $table_id]);
}
```

---

### **الخطوة 7: العودة للطاولات** 🔙
```
بعد الحفظ:
         ↓
العودة إلى tables.php
         ↓
الآن "طاولة 5" أصبحت:
- 🔴 حمراء (مشغولة)
- الإجماليات تظهر: 45.00 ج
- الأصناف تظهر في الجدول
```

---

## 🔄 السيناريو الثاني: تعديل طلب موجود

### **الخطوة 1: عرض الطاولات** 📋
```
المستخدم → يفتح tables.php
         ↓
يرى "طاولة 5" حمراء 🔴 (مشغولة)
```

---

### **الخطوة 2: اختيار الطاولة** 👆
```
المستخدم → ينقر على "طاولة 5"
         ↓
AJAX → جلب طلب الطاولة من قاعدة البيانات
         ↓
عرض معلومات الطلب:
- الإجمالي: 50.00 ج
- الخصم: 5.00 ج
- الصافي: 45.00 ج
- الأصناف: شاي، قهوة، ...
```

**الكود:**
```javascript
// في tables.php
function selectTable(tableId, tableName) {
    loadTableOrder(tableId, tableName);
}

$.ajax({
    url: 'ajax/get_table_order.php',
    data: { table_id: tableId, table_name: tableName },
    success: function(response) {
        if (response.order) {
            // عرض بيانات الطلب
            displayOrderItems(response.items);
            displayTotals(response.order);
        }
    }
});
```

---

### **الخطوة 3: تعديل الطلب** ✏️
```
المستخدم → ينقر "تعديل الطلب"
         ↓
فتح pos_barcode.php مع:
- تحميل الطلب الموجود
- تحميل جميع الأصناف
- الإجماليات
```

**الكود:**
```javascript
// في pos.js - تحميل طلب موجود
function loadTableOrder(tableName, tableId) {
    $.ajax({
        url: 'ajax/get_table_order.php',
        data: { table_name: tableName, table_id: tableId },
        success: function(response) {
            if (response.order) {
                $('#current_order_id').val(response.order.id);
                
                // تحميل الأصناف
                response.items.forEach(function(item) {
                    addRowToTable(item);
                });
                
                updateTotal();
            }
        }
    });
}
```

---

### **الخطوة 4: إضافة/تعديل أصناف** 📝
```
المستخدم يمكنه:
✓ إضافة أصناف جديدة
✓ تعديل الكميات
✓ تعديل الأسعار
✓ حذف أصناف
✓ تعديل الخصم
```

---

### **الخطوة 5: حفظ التعديلات** 💾
```
المستخدم → ينقر "حفظ"
         ↓
Form → do/doadd_invoice.php
         ↓
تحديث الطلب الموجود:

1. UPDATE ot_head (تحديث الإجماليات)
2. DELETE من fat_details (حذف الأصناف القديمة)
3. INSERT في fat_details (إضافة الأصناف الجديدة)
```

**الكود في do/doadd_invoice.php:**
```php
$order_id = $_POST['order_id'] ?? null;

if ($order_id) {
    // تحديث الطلب الموجود
    $stmt = $conn->prepare("UPDATE ot_head 
        SET fat_total = ?, fat_disc = ?, pro_value = ? 
        WHERE id = ?");
    $stmt->execute([$total, $discount, $net, $order_id]);
    
    // حذف الأصناف القديمة
    $conn->query("UPDATE fat_details SET isdeleted = 1 WHERE pro_id = $order_id");
    
    // إضافة الأصناف الجديدة
    foreach ($items as $item) {
        $stmt = $conn->prepare("INSERT INTO fat_details 
            (pro_id, item_id, qty, price) VALUES (?, ?, ?, ?)");
        $stmt->execute([$order_id, $item['id'], $item['qty'], $item['price']]);
    }
} else {
    // طلب جديد (نفس السيناريو الأول)
}
```

---

## 🎯 السيناريو الثالث: إنهاء الطلب والدفع

### **الخطوة 1: حساب المبلغ** 💰
```
في pos_barcode.php:
┌──────────────┬──────────┐
│ الإجمالي     │ 150.00 ج │
│ خصم          │  15.00 ج │
│ الصافي       │ 135.00 ج │
│ المدفوع      │ 150.00 ج │
│ الباقي       │  15.00 ج │ (مرتجع)
└──────────────┴──────────┘
```

---

### **الخطوة 2: الدفع** 💳
```
المستخدم → ينقر "حفظ وطباعة"
         ↓
1. حفظ الطلب النهائي
2. طباعة الفاتورة
3. تحديث الطاولة:
   - table_case = 0 (فارغة)
   - current_order_id = NULL
```

---

### **الخطوة 3: إغلاق الطلب** ✅
```
بعد الدفع:
         ↓
الطاولة ترجع:
- 🟢 خضراء (فارغة)
- جاهزة لطلب جديد
```

---

## 📊 قاعدة البيانات - الجداول المستخدمة

### **1. tables** - الطاولات
```sql
id | tname      | table_case | current_order_id | isdeleted
1  | طاولة 1    | 0          | NULL            | 0
2  | طاولة 2    | 1          | 123             | 0
3  | طاولة 3    | 0          | NULL            | 0
```

### **2. ot_head** - رأس الطلب
```sql
id  | pro_tybe | info      | fat_total | fat_disc | pro_value | pro_date
123 | 9        | طاولة 2   | 150.00    | 15.00    | 135.00    | 2025-10-16
```

### **3. fat_details** - تفاصيل الطلب
```sql
id  | pro_id | item_id | qty  | price | isdeleted
1   | 123    | 45      | 2    | 15.00 | 0
2   | 123    | 67      | 1    | 25.00 | 0
3   | 123    | 89      | 3    | 10.00 | 0
```

### **4. myitems** - الأصناف
```sql
id | barcode | iname  | price1 | group1 | isdeleted
45 | 123456  | شاي    | 15.00  | 1      | 0
67 | 789012  | قهوة   | 25.00  | 1      | 0
89 | 345678  | عصير   | 10.00  | 2      | 0
```

---

## 🔄 الدورة الكاملة - Flow Chart

```
START
  ↓
[عرض الطاولات - tables.php]
  ↓
[اختيار طاولة] ──→ فارغة? ──→ نعم ──→ [طلب جديد]
  ↓                                        ↓
  لا                                    [POS - إضافة أصناف]
  ↓                                        ↓
[تحميل الطلب الموجود]                   [حساب الإجماليات]
  ↓                                        ↓
[تعديل الطلب]                           [حفظ في DB]
  ↓                                        ↓
[إضافة/حذف أصناف]                       [تحديث حالة الطاولة]
  ↓                                        ↓
[حفظ التعديلات]                         [العودة للطاولات]
  ↓                                        ↓
[هل انتهى الطلب?] ──→ نعم ──→ [الدفع والطباعة]
  ↓                                        ↓
  لا                                    [تفريغ الطاولة]
  ↓                                        ↓
[العودة للـ POS]                         END
  ↓
[الاستمرار في التعديل]
```

---

## 🎨 الملفات والمسؤوليات

### **1. tables.php** 📋
- عرض جميع الطاولات
- عرض حالة كل طاولة
- عرض تفاصيل الطلب المختار
- زر للذهاب للـ POS

### **2. pos_barcode.php** 🖥️
- إضافة الأصناف (باركود أو نقر)
- تعديل الكميات والأسعار
- حساب الإجماليات
- حفظ الطلب
- زر للعودة للطاولات

### **3. pos_tables.php** 🎯
- نظام متكامل في شاشة واحدة
- عرض الطاولات + POS معاً
- AJAX كامل

### **4. ajax/get_tables.php** 📡
- جلب قائمة الطاولات
- JSON Response

### **5. ajax/get_table_order.php** 📡
- جلب طلب طاولة معينة
- جلب الأصناف والإجماليات

### **6. ajax/save_order.php** 💾
- حفظ الطلب الجديد
- تحديث الطلب الموجود
- تحديث حالة الطاولة

### **7. do/doadd_invoice.php** 💾
- معالجة Form الحفظ
- الحفظ في قاعدة البيانات
- إعادة التوجيه

### **8. js/pos.js** ⚡
- إدارة الأصناف
- الحسابات التلقائية
- نظام الطاولات
- AJAX Handlers

---

## 🚀 الخلاصة - Complete Workflow

```
1. المستخدم يفتح tables.php
2. يختار طاولة (فارغة أو مشغولة)
3. ينقر "طلب جديد" أو "تعديل الطلب"
4. يفتح pos_barcode.php
5. يضيف/يعدل الأصناف
6. الحسابات تتم تلقائياً
7. يحفظ الطلب
8. الطاولة تتحول للأحمر (مشغولة)
9. يمكن العودة للتعديل في أي وقت
10. عند الانتهاء: الدفع والطباعة
11. الطاولة تعود للأخضر (فارغة)
12. جاهزة لطلب جديد
```

---

**✅ النظام متكامل وجاهز للاستخدام!** 🎉

